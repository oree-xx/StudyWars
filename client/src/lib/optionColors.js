// Shared Kahoot-style option colors, used by both the host's live view
// and the player's answer buttons so the two screens visually match.
export const OPTION_COLORS = ['bg-red-500', 'bg-blue-500', 'bg-yellow-500', 'bg-emerald-500']
